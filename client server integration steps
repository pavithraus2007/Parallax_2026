STEPS TO BE FOLLOWED FOR CLIENT SERVER INTEGRATION 
Pre Requisites 
create a folder in vs code
http://172.16.45.104:8000/docs run this in chrome an interface will be shown to u this isn't an imp step servers ip 
pip install python
python -m pip install requests
"netsh wlan show interfaces" run in terminal { to showcase windows telimentries}
// 
then  run 
when client runs the code  the server should not kill the pgm(fastapi server)

python -m pip install requests
netsh wlan show interfaces

